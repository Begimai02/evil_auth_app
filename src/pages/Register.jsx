import React from "react";
import Wrapper from "../components/Wrapper";

const Register = () => {
  return (
    <div>
      <Wrapper title="Sign Up" imgOrder={2} />
    </div>
  );
};

export default Register;
